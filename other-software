#
# Edit this list with the packages you want to install,
# then save this file and quit.
#

# Ruby packages
sudo gem install lolcat lolcommits

# Pip packages
pip install thefuck

# Atom extensions
apm install file-icons emmet highlight-selected

# Calibre
sudo -v && wget -nv -O- https://download.calibre-ebook.com/linux-installer.sh | sudo sh /dev/stdin

# Low battery notification
wget https://github.com/rjekker/i3-battery-popup/raw/master/i3-battery-popup -o $HOME/.local/bin/i3-battery-popup
chmod +x $HOME/.local/bin/i3-battery-popup

